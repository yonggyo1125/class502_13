const todo = {
    /**
     * 스케줄 추가 
     * 
     */
    add() {

    },
    /**
     * 스케줄 삭제
     * 
     */
    remove() {

    }
};

window.addEventListener("DOMContentLoaded", function () {
  frmRegist.addEventListener("submit", function (e) {
    e.preventDefault();

  });
});
